import:
  types:
    tcp_transport:
      - route
  code:
    - transport.tcp
require:
  - ref_collector
  - unbundler
  - parcel_registry
  - route_table
  - transport
  - service_failure
provide:
  - tcp_server
