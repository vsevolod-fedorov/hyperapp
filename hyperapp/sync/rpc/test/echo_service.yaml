import:
  types:
    rpc:
      - endpoint
    echo:
      - echo_iface
  code:
    - sync.rpc.rpc_endpoint
require:
  - unbundler
  - generate_rsa_identity
  - rpc_endpoint
  - endpoint_registry
  - rpc_proxy
