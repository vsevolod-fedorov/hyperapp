sample_model = record

sample_item = record:
  id: int

sample_record = record:
  id: int
  text: string

sample_crud_get_action = record
sample_crud_update_action = record

sample_selector = record
sample_selector_model = record
sample_selector_item = record

sample_selector_open_action = record
sample_selector_pick_action = record

selector_view = record

save_d = record
