model = record:
  ui_t: ref
  impl: ref


list_ui_t = record:
  element_t: ref

tree_ui_t = record:
  element_t: ref

record_ui_t = record:
  record_t: ref


fn_impl = record:
  function: ref
  ctx_params: string list
  service_params: string list
