view = record

item = record:
  id: int
  name: string
  focusable: bool
  description: string


command_list = record:
  item_id: int

command_item = record:
  name: string


layout_command_impl = record:
  ui_command_impl: ref


open_layout_tree_d = record
add_view_command_d = record
open_view_item_commands_d = record
view_item_commands_d = record
enum_layout_tree_commands_d = record
