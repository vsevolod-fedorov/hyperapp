
view = record:
  current_view: ref
  current_model: ref
  commands: ref list
  prev: ref opt
  next: ref opt

state = record:
  current_state: ref
  prev: ref opt
  next: ref opt


open_new_diff = record:
  new_current: ref
  new_model: ref
  commands: ref list

go_back_diff = record
go_forward_diff = record
