
history_rec = record:
  view: ref
  model: ref
  state: ref
  prev: ref opt
  next: ref opt

view = record:
  current_view: ref
  current_model: ref
  prev: ref opt
  next: ref opt


go_back_d = record
go_forward_d = record
