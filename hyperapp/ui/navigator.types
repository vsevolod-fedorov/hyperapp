
view = record:
  current_view: ref
  current_model: ref
  prev: ref opt
  next: ref opt

state = record:
  current_state: ref
  prev: ref opt
  next: ref opt


go_back_d = record
go_forward_d = record

go_back_diff = record
go_forward_diff = record
