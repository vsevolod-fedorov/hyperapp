
pos = record:
  x: int
  y: int

size = record:
  w: int
  h: int

layout = record:
  menu_bar_ref: ref
  command_pane_ref: ref
  central_view_ref: ref

state = record:
  size: size
  pos: pos

app_layout = record:
  window_layout: ref

app_state = record:
  window_list: ref list
