
element = record:
  view: ref opt  # A stretch if none.
  focusable: bool
  stretch: int

view = record:
  direction: string  # LeftToRight, TopToBottom, etc.
  elements: element list

state = record:
  current: int
  elements: ref opt list  # None for stretch elements.
