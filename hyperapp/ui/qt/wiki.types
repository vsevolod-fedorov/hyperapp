wiki_ref = record:
  id: string
  target: ref

wiki = record:
  text: string
  refs: wiki_ref list

wiki_to_string_convertor = record


ref_list_adapter = record:
  accessor: ref

ref_list_item = record:
  id: string
  target: ref
  title: string

ref_list_model_state = record:
  current_id: string
  current_item: ref_list_item


text_view = record:
  adapter: ref

wiki_view = record:
  adapter: ref

state = record
