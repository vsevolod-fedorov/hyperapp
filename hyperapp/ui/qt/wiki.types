wiki_ref = record:
  id: string
  target: ref

wiki = record:
  text: string
  refs: wiki_ref list

wiki_to_string_convertor = record

ref_list_adapter = record:
  accessor: ref

ref_list_item = record:
  id: string
  target: ref
  title: string

text_view = record:
  adapter: ref

wiki_view = record:
  adapter: ref

state = record
