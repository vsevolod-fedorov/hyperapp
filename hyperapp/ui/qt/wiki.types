wiki_ref = record:
  id: string
  target: ref

wiki = record:
  text: string
  refs: wiki_ref list

adapter = record

text_view = record:
  adapter: ref

wiki_view = record:
  adapter: ref

state = record
