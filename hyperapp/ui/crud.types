model = record:
  value_t: ref
  model: ref
  keys: ref list
  key_fields: string list
  init_action_fn: ref
  commit_command_d: ref
  get_fn: ref opt
  pick_fn: ref opt
  commit_action_fn: ref


open_command_fn = record:
  name: string
  value_t: ref
  key_fields: string list
  init_action_fn: ref
  commit_command_d: ref
  commit_action_fn: ref

init_fn = record

edit_d = record
save_d = record
