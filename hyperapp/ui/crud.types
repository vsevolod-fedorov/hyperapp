model = record:
  record_t: ref
  model: ref
  key: ref
  key_field: string
  init_action_fn: ref
  commit_command_d: ref
  commit_action_fn: ref


open_command_fn = record:
  name: string
  record_t: ref
  key_field: string
  init_action_fn: ref
  commit_command_d: ref
  commit_action_fn: ref

init_fn = record

edit_d = record
save_d = record
