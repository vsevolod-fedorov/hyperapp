from build import python_module_src

job = record:
  python_module: python_module_src
  idx: int
  deps: ref list

succeeded_result = record:
  requirements: ref list

incomplete_result = record:
  requirements: ref list
  message: string
  traceback: string list

error_result = record:
  message: string
  traceback: string list
