service_probe_resource = record:
  service_name: string
  function: ref
  params: string list

fixture_probe_resource = record:
  service_name: string
  function: ref
  params: string list

service_template_resource = record:
  service_name: string
  function: ref
  free_params: string list
  service_params: string list
  want_config: bool
