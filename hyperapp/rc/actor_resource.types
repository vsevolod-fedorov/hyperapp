cfg_item_req = record:
  service_name: string
  actor: ref
  tested_modules: string list

actor_probe_ctr = record:
  module_name: string
  attr_qual_name: string list
  service_name: string
  t: ref

ctx_actor_probe_ctr = record:
  module_name: string
  attr_qual_name: string list
  service_name: string
  t: ref
  has_params: bool

actor_template_ctr = record:
  module_name: string
  attr_qual_name: string list
  service_name: string
  t: ref
  creg_params: string list  # Not used by constructor.
  service_params: string list

ctx_actor_template_ctr = record:
  module_name: string
  attr_qual_name: string list
  service_name: string
  t: ref
  ctx_params: string list
  service_params: string list

actor_probe_template = record:
  function: ref

ctx_actor_probe_template = record:
  function: ref
