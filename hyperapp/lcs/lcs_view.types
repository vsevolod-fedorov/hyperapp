view = record:
  filter: ref list  # d list

item = record:
  layer_d: ref
  layer: string
  dir: ref list
  piece: ref
  dir_str: string
  piece_str: string

lcs_remove_d = record
lcs_open_piece_d = record
