import:
  types:
    service:
      - list_service
    rpc_command:
      - rpc_command
    server_ref_list:
      - row
require:
  - resource_registry
  - python_object_creg
  - local_server_ref
  - server_rpc_endpoint
