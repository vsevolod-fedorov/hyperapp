definitions:

  config:
    type: legacy_type.system:system_config
    value:
      services:
        transport: transport.config

  transport.config:
    type: base:transport/transport.t:config
    value:
      message_size_limit: 102400
