import:
  - legacy_module.server.htest_runner
  - legacy_service.mosaic
  - legacy_service.types
  - legacy_service.python_object_creg

definitions:

  runner_attribute:
    type: attribute
    object: legacy_module.server.htest_runner
    attr_name: Runner

  runner_factory:
    type: partial
    function: runner_attribute
    params:
      mosaic: legacy_service.mosaic
      types: legacy_service.types
      python_object_creg: legacy_service.python_object_creg

  runner:
    type: call
    function: runner_factory

  runner_method_collect_attributes:
    type: attribute
    object: runner
    attr_name: collect_attributes

  get_function_result_type:
    type: attribute
    object: runner
    attr_name: get_function_result_type
