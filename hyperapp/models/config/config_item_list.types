model = record:
  layer: string opt
  service_name: string

item = record:
  key: ref
  key_str: string
  value: ref opt  # None for non-data values.
  value_str: string
  layers: string list
  layers_str: string
