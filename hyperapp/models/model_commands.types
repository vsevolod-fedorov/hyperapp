model_commands = record:
  model: ref
  model_state: ref

item = record:
  command_d: ref
  name: string
  repr: string

open_model_commands_d = record
list_model_commands_d = record
run_command_d = record
