view = record:
  model: ref
  model_state: ref

item = record:
  ui_command_d: ref
  model_command_d: ref
  name: string
  groups: string
  repr: string

open_model_commands_d = record
run_command_d = record
