item = record:
  name: string
  groups: string
  shortcut: string
  text: string
  tooltip: string

form = record:
  text: string
  tooltip: string

command = record:
  name: string
  key: ref
  model: ref
  model_state:ref

model = record:
  commands: command list
