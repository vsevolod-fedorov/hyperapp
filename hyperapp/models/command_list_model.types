item = record:
  name: string
  groups: string
  shortcut: string
  text: string
  tooltip: string
  command: ref

form = record:
  text: string
  tooltip: string

command = record:
  name: string
  key: ref
  model: ref opt
  model_state: ref opt

model = record:
  commands: command list
