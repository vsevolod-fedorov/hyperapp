item = record:
  name: string
  groups: string
  shortcut: string
  text: string
  tooltip: string
  bound_command: ref

form = record:
  text: string
  tooltip: string

bound_command = record:
  name: string
  key: ref
  model: ref opt
  model_state: ref opt

model = record:
  commands: bound_command list
