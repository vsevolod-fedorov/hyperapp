item = record:
  ui_command_d: ref
  model_command_d: ref
  name: string
  groups: string
  repr: string
  shortcut: string
  text: string
  tooltip: string

form = record:
  text: string
  tooltip: string

set_shortcut_d = record
