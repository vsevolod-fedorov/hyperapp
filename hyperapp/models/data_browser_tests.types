sample_model_1 = record
sample_model_2 = record

inner_data = record:
  text: string

sample_data = record:
  name: string
  sample_list: int list
  inner: inner_data
  value: ref

sample_opt_data = record:
  str_field: string opt
