base:
rc:
  imports: [base]
common:
  imports: [base]
ui:
  imports: [base, common]
models:
  imports: [base, common, ui]
lcs:
  imports: [base, common, ui, models]
views:
  imports: [base, common, ui]
client:
  imports: [base, common, ui]
server:
  imports: [base, common, ui]
