import:
  types:
    lcs:
      - storage
      - lcs_association
      - lcs_set_association
      - lcs_resource_association
      - lcs_set_resource_association
require:
  - file_bundle
  - aux_bundler_hooks
  - aux_unbundler_hooks
  - meta_registry
  - python_object_creg
provide:
  - lcs
