element = record:
  key: string


container = record:
  element_field: element

list_container = record:
  element_field: element list

opt_container = record:
  element_field: element opt

based_container = record(container)


base_interface = interface:
  some_method()

derived_interface = interface(base_interface):
  another_method()
