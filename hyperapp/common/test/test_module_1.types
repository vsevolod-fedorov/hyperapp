some_int = int
some_string_opt = string opt
some_bool_list = bool list
some_bool_list_opt = some_bool_list opt

record_1 = record:
  int_field: int

record_2 = record(record_1):
  string_field: string


object = hierarchy object

object_base = object class base:
  impl_id: string

handle = hierarchy handle


simple_class = object class simple_2

text_object = object class text_2(simple_class):
  text: string

params_form_iface = interface test_list_params_2:
  diff_type: string
  contents:
    contents_1: string
    contents_2: int
  commands:
    submit (key: int, size: int) -> (handle: handle opt)

test_iface_a = interface iface_a:
  commands:
    params () -> (handle: handle opt)
