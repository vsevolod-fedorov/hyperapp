
lcs_association = record:
  dir: ref list
  value: ref

lcs_set_association = record:
  dir: ref list
  value: ref

storage = record:
  association_list: ref list


lcs_resource_association = record:
  dir: ref list
  value: ref

lcs_set_resource_association = record:
  dir: ref list
  value: ref
