from core import handle, view_handle
from packet import error, client_error


hash = binary
ref = hash
full_type_name = string list

referred = record:
    full_type_name: full_type_name
    hash_algorithm: string
    encoding: string
    encoded_object: binary
    

unknown_ref_error = error class unknown_ref(client_error):
    ref: ref

ref_resolver = interface ref_resolver:
    commands:
        request resolve_ref (ref: ref) -> (referred: referred)
