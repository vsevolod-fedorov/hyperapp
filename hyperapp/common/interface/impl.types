
list_spec = record:
  key_attribute: string
  key_t: ref
  dir: ref

string_spec = record


impl_association = record:
  piece_t: ref
  ctr_fn: ref opt
  spec: ref


service_provider = record:
  service: ref
  peer: ref

method_command_impl = record:
  object_ctr: ref  # Unused.
  method: string
  params: string list
  dir: ref

object_command_impl = record:
  function: ref
  params: string list
  dir: ref

global_command_impl = record:
  function: ref
  dir: ref
