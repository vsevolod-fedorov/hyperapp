from core import handle
from error import error, client_error


test_error = error class test(client_error):
    code: int


test_object = list_interface test_object:
    columns:
        @id: string
        title: string
    commands:
        open (element_key: string) -> (handle: handle opt)
        run (element_key: string) -> ()
