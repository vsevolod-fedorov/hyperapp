
param_type = record:
  id: string
  type_ref: ref

parameter = record:
  id: string
  param_ref: ref

command = record:
  id: string
  command_ref: ref

service = record:
  type_ref: ref
  peer_ref: ref
  object_id: string
  param_type_list: param_type list
  param_list: parameter list
  command_list: command list


list_service = record(service)


record_field = record:
  id: string
  type_ref: ref

record_service = record(service):
  field_list: record_field list
