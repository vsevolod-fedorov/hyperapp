
command = record:
  id: string
  code_id: string
  layout_ref: ref opt


layout = record:
  command_list: command list


default_layout_handle = record:
  object_type_ref: ref

command_layout_handle = record:
  base_handle_ref: ref
  command_id: string
