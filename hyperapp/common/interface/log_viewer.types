from core import object, object_base


log_viewer = object class log_viewer(object_base):
    session_id: string

log_record = object class log_record(object_base):
    session_id: string
    item_path: int list

log_record_constructor = record:
    session_id: string
