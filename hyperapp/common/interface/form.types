from core import object, object_base, handle, obj_handle


form_object = object class form(object_base)


form_view_field = record:
    id: string
    view: handle

form_handle = handle class form(obj_handle):
    field_list: form_view_field list
    current_field_id: string
