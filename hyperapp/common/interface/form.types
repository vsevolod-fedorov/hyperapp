from core import handle, view_handle


form = hierarchy form

form_base = form class base:
    impl_id: string


form_view_field = record:
    id: string
    view: handle

form_handle = handle class form(view_handle):
    object: form
    field_list: form_view_field list
    mode: string
    current_field_id: string
