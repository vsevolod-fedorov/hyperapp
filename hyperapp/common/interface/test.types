from error import error, client_error


test_error = error class test(client_error):
    error_message: string

packet = record:
    message: string

echo = interface echo:
    commands:
        request say (message: string) -> (response: string)
        request eat (message: string) -> ()
        notification notify (message: string) -> ()
        request fail (message: string) -> ()

echo_notifications = interface echo_notifications:
    commands:
        notification changed (message: string) -> ()
