import:
  types:
    lcs:
    - lcs_association
    - lcs_set_association
    - lcs_resource_association
    - lcs_set_resource_association
  code:
  - common.lcs
require:
- aux_bundler_hooks
- meta_registry
provide:
- lcs
