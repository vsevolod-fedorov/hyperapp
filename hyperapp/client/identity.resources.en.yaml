ThisModule:
  commands:
    identity_list:
      text: Identities
      description: Open identity list
      shortcuts:
      - Alt+I
    create_identity:
      text: Create identity
      description: Create new identity, public+private key pair
      shortcuts:
      - Alt+N
IdentityList:
  columns:
    name:
      text: Identity name
      description: Identity name
  commands:
    create:
      text: Create
      description: Create new identity, generate private+public key pair
      shortcuts:
      - Ins
IdentityFormObject:
  commands:
    submit:
      text: Create
      description: Create new identity, generate private+public key pair
      shortcuts:
      - Return
