import:
  types:
    lcs:
      - lcs_association
      - lcs_set_association
  code:
  - common.lcs
require:
  - file_bundle
  - aux_bundler_hooks
  - aux_unbundler_hooks
  - meta_registry
provide:
  - lcs
