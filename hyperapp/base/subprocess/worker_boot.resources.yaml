definitions:

  worker_boot.module:
    type: builtin:type:python_module
    value:
      module_name: worker_boot
      file_name: worker_boot.dyn.py
      imports:
        pyobj:
          code.system: :system/system:system.module
        raw:
          data.subprocess.config.config: config:config
          data.main_svc: worker_main:subprocess_worker_main.service

  boot:
    type: builtin:type:attribute
    value:
      object: worker_boot.module
      attr_name: boot
