definitions:

  subprocess.module:
    type: builtin:type:python_module
    value:
      module_name: subprocess
      file_name: subprocess.dyn.py
      imports:
        pyobj:
          services.mosaic: builtin:service:mosaic
        raw:
          data.subprocess.mp_main: mp_main.py

  mp_main.py:
    type: builtin:type:file_data
    value:
      file_name: mp_main.py

  subprocess_running:
    type: builtin:type:attribute
    value:
      object: subprocess.module
      attr_name: subprocess_running

  subprocess_running.service.t:
    type: builtin:type:record_mt
    value:
      name: subprocess_running
      base: null
      fields: {}

  subprocess_running.service:
    type: subprocess_running.service.t
    value: {}

  subprocess_running.service-actor:
    type: :system/system.t:service_actor
    value:
      fn: subprocess_running
      adapters:
      - subprocess_running.service-args-adapter
      - :system/adapter:service_object.adapter

  subprocess_running.service-args-adapter:
    type: :system/adapter.t:service_args_adapter
    value:
      args:
        bundler: :bundler:bundler.service
