definitions:

  worker.module:
    type: builtin:type:python_module
    value:
      module_name: worker
      file_name: worker.dyn.py
      imports:
        pyobj:
          code.make_partial: :make_partial:make_partial.module
          code.futures: futures:futures.module
        raw:
          data.worker_boot.boot: worker_boot:boot

  subprocess_workers_running:
    type: builtin:type:attribute
    value:
      object: worker.module
      attr_name: subprocess_workers_running

  subprocess_workers_running.service.t:
    type: builtin:type:record_mt
    value:
      name: subprocess_workers_running
      base: null
      fields: {}

  subprocess_workers_running.service:
    type: subprocess_workers_running.service.t
    value: {}

  subprocess_workers_running.service-actor:
    type: :system/system.t:service_actor
    value:
      fn: subprocess_workers_running
      adapters:
      - subprocess_workers_running.service-args-adapter
      - :system/adapter:service_object.adapter

  subprocess_workers_running.service-args-adapter:
    type: :system/adapter.t:service_args_adapter
    value:
      args:
        transport: :transport/transport:transport.service
        subprocess_running: subprocess_running:subprocess_running.service
