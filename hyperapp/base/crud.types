arg = record:
  name: string
  value: ref

model = record:
  value_t: ref
  model: ref opt
  args: arg list
  init_action_fn: ref opt
  commit_command_d: ref
  get_fn: ref opt
  pick_fn: ref opt
  commit_action_fn: ref
  commit_value_field: string


open_command_fn = record:
  name: string
  value_t: ref
  key_fields: string list
  init_action_fn: ref
  commit_command_d: ref
  commit_action_fn: ref

init_fn = record

edit_d = record
save_d = record

str_adapter = record
