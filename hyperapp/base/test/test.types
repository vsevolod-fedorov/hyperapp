empty = record

simple = record:
  id: int

big = record:
  value: string

ref_list = record:
  elements: ref list

derived = record(simple):
  value: string

complex_1 = record:
  inner: simple

complex_2 = record:
  simple: simple
  derived: derived

rec_list = record:
  elements: simple list
