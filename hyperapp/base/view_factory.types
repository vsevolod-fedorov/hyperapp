template = record:
  k: ref
  view_t: ref
  is_wrapper: bool
  view_ctx_params: string list
  system_fn: ref

item = record:
  k: ref
  k_str: string
  view_t: ref
  view_t_str: string
  is_wrapper: bool
  view_ctx_params: string list
  system_fn: ref

factory = record:
  model_t: ref opt
  k: ref opt
