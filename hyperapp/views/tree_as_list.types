list_model = record:
  tree_model: ref
  tree_model_fn: ref
  current_path: ref list
  parent_item: ref opt

view = record:
  list_view: ref
  tree_model_fn: ref
  current_path: ref list
  parent_items: ref list

index_view = record(view)
key_view = record(view)
